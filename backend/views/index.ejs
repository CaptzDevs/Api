<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Captz API</title>
</head>
<style>
    *{
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }
    body{
        min-height: 100vh;
    }
    main{
        min-height: 100vh;
        background: #323232;
        color: white;
        display: flex;
        align-items: center;
        justify-content: flex-start;
        flex-direction: column;
        padding-bottom: 50px;
        gap: 20px;
    }
    
   section.banner{
        width: 100%;
        min-height: 50vh;
        height: auto;
        background: #373737;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-wrap: wrap;
        font-size: 2rem;
        gap: 20px;
   }

   section.apis{
        width: 80%;
        background: rgb(57, 57, 57);
        display: flex;
        align-items: flex-start;
        justify-content: flex-start;
        padding: 20px;
        gap: 20px;
        border-radius: 10px;
        flex-wrap: wrap;
   }

   .api-item{
        padding: 20px;
        border-radius: 10px;
        display: flex;
        align-items: flex-start;
        justify-content: flex-start;
        flex-direction: column;
        gap: 20px;
        border: 1px solid white;
        flex: 1 0 300px;


   }
   .api-links {
        display: flex;
        gap: 10px;
        
   }
    .api-link{
        border: 1px solid white;
        padding: 10px;
        border-radius: 5px;
        color: white !important;

    }

   a{
        color: rgb(60, 60, 71);
        text-decoration: none;
   }
   .banner-img{
        width: 100%;
        height: 100%;
        max-width: 300px;
        max-height: 300px;
   }
   .banner-img img{
        width: 100%;
        height: 100%;
        object-fit: cover;
        user-select: none;
        pointer-events: none;
   }
</style>
<body>

    <main>
        <section class="banner">
            <div class="banner-img">
                <img src="/image/Hacker-amico-nobg.svg" alt="">
            </div>
            <h1> Captz's API</h1>

        </section>

        <h1>Avaliable API(s)</h1>
        <section class="apis">
            <% data.map((item,i) =>{ %>

            <div class="api-item">
                <h2><%=/*  i+1+'. '+ */item.name %></h2>

                   <div class="api-links">
                       <a class="api-link" href="<%= item.endpoint %>" target="_blank">Json </a>
                       <a class="api-link" href="<%= item.view %>" target="_blank">View </a>
                       
                   </div>

             </div>

            <% }) %>
        </section>

    </main>

    
</body>
</html>