<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    *{
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }
    main{
        background: #323232;
    }
    .music-section{
        display: flex;
        align-items: flex-start;
        justify-content: center;
        flex-direction: column;
        gap: 10px;
        padding: 20px;
    }
    .music-item{
        width: 100%;
        min-height: 150px;
        padding: 20px;
        display: flex;
        align-items: flex-start;
        justify-content: flex-start;
        background: #404040;
        border-radius: 10px;
        color: white;
        gap: 10px;
    }
    .music-item-body{
        display: flex;
        align-items: flex-start;
        justify-content: flex-start;
        flex-direction: column;
        gap: 10px;
        font-size: 2rem;
    }
    .music-artist{
        width: 100%;
        font-size: .8rem;
    }
    .music-item img{
        border-radius: 10px;
    }
    .music-rank{
        padding: 20px;
        font-size: 2rem;
    }
</style>
<body>

    <main>
        <section class="music-section">
            <% data.map((item,i)=>{ %>
            <div class="music-item">
                <div class="music-rank"><%= item.rank %></div>
                <img src="<%= item.img %>" alt="" srcset="">
                <div class="music-item-body">
                    <span class="music-song">  <%= item.song  %></span>
                    <span class="music-artist">  <%= item.artist %></span>
                </div>
            </div>
            <% }) %>
        </section>
</main>

    
</body>

</html>